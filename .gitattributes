# 이 파일은 9대의 이기종 컴퓨터들에 터미널 명령어 숙지 여부와 상관없이
# 강제로 리눅스 계열 표준 줄바꿈(LF)을 이식하여 운영체제 에러를 봉쇄하는 절대 규약입니다.

# 모든 텍스트 파일을 Git이 클라이언트 환경(OS)과 무관하게 표준 LF 포맷으로 저장 및 인식하도록 통제합니다.
* text=auto eol=lf

# 윈도우 환경 전용으로 작성되는 특수한 배치 파일(*.bat)만큼은 예외 처리로 윈도우(CRLF) 줄바꿈을 허가합니다.
*.bat text eol=crlf
